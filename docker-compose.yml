version: '3.8'

services:
  db:
    image: postgres:15
    container_name: pg-container
    environment:
      POSTGRES_PASSWORD: admin
    ports:
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
